# src/features

Responsabilitat:
- Agrupar funcionalitats per domini (UI + actions específiques).

Regles:
- `ui/`: presentació i interacció.
- `actions/`: entrada server-side validada.
- Lògica de negoci real a `src/services`.
